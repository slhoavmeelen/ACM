#include <stdio.h>

char jibie[] = {'S','B','Q','W','S','B','Q','Y'};

int main(){
	char res[18+1];
	int num,temp,i=0,idx=0;
	bool isFirstZero = true,isFirstCh = true;
	scanf("%d",num);
	while(num > 0){
		temp = num%10;
		if(temp==0){
			if(isFirstZero){
				
			}else{ 
				
			}
		}else{
			res[idx] = temp
		}
	}
	 
	return 0;
}
